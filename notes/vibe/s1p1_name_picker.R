# STAT 5400 - Section 1.1 - Vibe task: the name picker
# Open in a Codespace with Copilot on.
#
# TASK
#  1. teams(names, size = 4): split a character vector into random teams of `size`,
#     no repeats, last team may be smaller. Return a list.
#  2. Make it a web page: one button, click = a new random name, no repeats until
#     everyone has been picked. (Shiny, or a plain HTML file with a few lines of JavaScript.)
#  3. Verify: every name appears exactly once across the teams.

names <- c("Ada", "Blaise", "Carl", "Daphne", "Egon", "Florence", "Gauss", "Hilda",
           "Irene", "Jerzy", "Karl", "Lena")

teams <- function(names, size = 4) {

}
